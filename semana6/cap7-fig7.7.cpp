#include<iostream>
#include<iomanip>
#include<array>

using namespace std;

int main(){
    const size_t arraySize{11};
    array<<usigned int; arraySize> n{0, 0, 0, 0, 0, 0, 1, 2, 4, 2, 1};

    cout << "Grade distribution: "<< endl ;

    for (size_t i{0})

}